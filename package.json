{
  "name": "pulseterminal-x",
  "version": "1.0.0",
  "description": "Advanced financial intelligence platform",
  "private": true,
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\" \"npm run dev:intelligence\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev:intelligence": "cd intelligence && uvicorn app.main:app --reload --port 8001",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "test": "npm run test:backend && npm run test:intelligence",
    "test:backend": "cd backend && npm test",
    "test:intelligence": "cd intelligence && pytest --cov=app --cov-report=term-missing",
    "lint": "cd frontend && npm run lint && cd ../backend && npm run lint",
    "db:migrate": "cd backend && npx prisma migrate dev",
    "db:generate": "cd backend && npx prisma generate",
    "db:seed": "cd backend && npx ts-node prisma/seed.ts"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
